F4 Display the items in the active list. Backspace Open a folder one level up if a folder is selected in the Save As or Open dialog box. Windows logo key keyboard shortcuts. The following table contains keyboard shortcuts that use the Windows logo key . Press this key To do this. Windows logo key Open or close the Start menu.