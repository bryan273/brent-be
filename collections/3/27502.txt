perimeter = 0; for i = 1:size(points, 1)-1. perimeter = perimeter + norm(points(i, :) - points(i+1, :)); end. perimeter = perimeter + norm(points(end, :) - points(1, :)); % Last point to first. fprintf('The perimeter of the polygon is %.3f\n', perimeter) (points is the set of coordinates) Area. function p_area = assignment_task_b(x,y) % Get the number of vertices.