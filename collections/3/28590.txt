The range() function is available in two sets of parameters, as listed below: Checkout Python Interview Questions. range(stop) This method will just take the end of the sequence and the function generates the sequence excluding the number specified in the stop input parameter. range([start],stop[,step])