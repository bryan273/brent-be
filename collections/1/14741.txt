To access BIOS configuration screens and change the system’s parameters: 1. Enter the BIOS Setup utility by pressing the F2 key while the system is performing the power-on self-test (POST). When BIOS is started, the main BIOS Setup menu screen is displayed. 2. Highlight the field to be modified using the arrow and Tab keys. Use the left and right arrow keys to move sequentially back and forth through the menu screens. Fields that can be reconfigured are displayed in color.