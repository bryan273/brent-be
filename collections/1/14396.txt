As opposed to the manual setup described above, you can also synchronize the system clock with a remote server over the Network Time Protocol (NTP). ~]# 1  ntpdate server_address...  Unless an error message is displayed, the system time should now be set.