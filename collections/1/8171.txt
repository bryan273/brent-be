We may want to check if the output of cmake includes the following. Now, let's make and install it: Then, we want to configure the system wide library search path. In the standard Ubuntu install, the opencv.conf file does not exist, so we need to create it. Let's open /etc/ld.so.conf.d/opencv.conf, then type in /usr/local/lib. Append the following two lines to the file, /etc/bash.bashrc.