How to Make a Bump Key. Using these values, you'll make your first cut using the first value (key shoulder to first cut), then each subsequent cut will be spaced at equal intervals using the second value (distance between centers). As a general rule, you cut to the indented line on the blank key.