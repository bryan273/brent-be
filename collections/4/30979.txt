In your text file, the format is known. Thus the only sensible solution is to correctly parse the date in the text file into a DateTime object and the properly write it in XML file using XML expected format as you want your XML to properly works with XML serailization or when importing XML into Excel or any other use.hus the only sensible solution is to correctly parse the date in the text file into a DateTime object and the properly write it in XML file using XML expected format as you want your XML to properly works with XML serailization or when importing XML into Excel or any other use.