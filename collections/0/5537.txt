1 Response.Redirect: This method is widely used and common, but it will only use the GET method in ASP.NET, and there is no way to do a POST using it.2  Server.Transfer: This method does a POST not GET, but ... have already an article Redirect and Post JSON Object in ASP.NET MVC [ ^ ] that do redirect and post in asp.net MVC but for a JSON object, same idea too, except this one uses javascript to submit the form (you create it manually) and bind the posted data to a ViewModel.