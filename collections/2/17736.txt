You need to move the .vhd file for your VM into Azure storage and use it to create a new VM. You can also create a brand new DC on the Azure Virtual Network and enable inbound replication.ou can run Azure virtual machines (VMs) in the Azure Infrastructure Services public cloud. The VMs run on Hyper-V and are stored as .vhd files. You can create new VMs from templates provided by the service or create them yourself on your own premises and upload the .vhd files to Azure.