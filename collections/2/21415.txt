1 Leave the Replace with field blank so it deletes the lines it finds that text in and doesn't replace it with something. 2  Click Replace All to the right of the dialog window and it will replace all lines that contain spam with a blank line. In the replace dialog window, where it says find what enter: .*spam.*. 2  That's dot star then the text or character string you want to search for followed by another dot star. 3  Replace the word spam in my example with the text you want to search for.